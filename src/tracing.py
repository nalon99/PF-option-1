# this file contains common functions for tracing the workflow of the agents or image parsing and capture
# each trace includes input, output, latency, tokens, cost
# wherever applicable custom metadata are: session_id, contract_id, agent names.