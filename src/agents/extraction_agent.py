# in a multiagent system where there are two distinct agents with separate prompts and responsibilities, 
# this is Agent 2: it receives Agent 1's output, extracts specific changes.
# Shows clear handoff mechanism.


class ExtractionAgent:

    def agent_extract_changes(self):
        pass
